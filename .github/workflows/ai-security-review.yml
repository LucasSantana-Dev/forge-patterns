name: AI Security Review

on:
  pull_request:
    branches: [main]

permissions:
  pull-requests: write
  contents: read

jobs:
  ai-review:
    uses: Forge-Space/.github/.github/workflows/reusable-ai-security-review.yml@6fbe56bc610ddf84b70f213c752b95196bcadd95
    with:
      exclude-directories: "dist/,node_modules/"
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
