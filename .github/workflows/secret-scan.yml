name: Secret Scanning

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]

jobs:
  secrets:
    uses: Forge-Space/.github/.github/workflows/reusable-secret-scan.yml@main
    permissions:
      contents: read
